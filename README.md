# ELEC0148 Sustainable Energy Lab Component
Run `main.py` to view the JV curves and Power-Voltage plots
- Make sure `CSV Data File.csv` is in the same directory as `main.py`
- `requirements.txt` contains the required libraries